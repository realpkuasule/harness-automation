# Harness v1.0.7 人工测试指南

**日期**: 2026-04-27
**测试项目**: `/Users/zhichao/tmp/harness-test`
**前提**: 项目已 `npm run build`，MCP Server 可正常运行

> 本指南仅包含**无法自动测试**的项（需人工判断、npm 账号、时间等待、跨组件集成等）。基础功能（evaluate_rules、confirm_decisions、generate_config 等）已被自动化测试覆盖，不在本文列出。

---

## 准备

```bash
# 清理测试目录
rm -rf /Users/zhichao/tmp/harness-test
mkdir -p /Users/zhichao/tmp/harness-test/src

# 创建带问题的源文件
cat > /Users/zhichao/tmp/harness-test/src/index.ts << 'TESTEOF'
console.log("debug line");
debugger;
function calc(x: number): number {
  if (x > 30000) return x * 2;
  return x + 100;
}
TESTEOF

# 创建 package.json
cat > /Users/zhichao/tmp/harness-test/package.json << 'TESTEOF'
{"name":"harness-test","version":"0.0.0"}
TESTEOF

# 构建 server
cd /Users/zhichao/claude/harness/mcp-server && npm run build
```

---

## TC01: Git 仓库评估（assess_suitability 完整流程）

**原因**: `assess_suitability` 检查 Git 历史、文件结构等。测试项目可能不在 Git 中，需要人工确认评估结果是否符合预期。

```bash
cd /Users/zhichao/tmp/harness-test
git init && git add -A && git commit -m "init"
```

调用 `assess_suitability`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`
- **analysisDepth**: `full`

检查项:
- [ ] 返回 `suitable` 为 true（有 Git 历史、有 src/ 结构）
- [ ] `score` 在合理范围（>50 表示适合）
- [ ] `warnings` 内容合理（项目刚初始化，应有 low 级别警告）
- [ ] `recommendations` 有可操作建议

```bash
# 再试 quick 深度
git tag v0.0.1
```
- [ ] `analysisDepth=quick` 返回更快、结果更简略
- [ ] `analysisDepth=full` 比 quick 返回更多细节

---

## TC02: 认知层技能调用 —— 输出质量主观评估

**原因**: 认知层（diagnostic/educational/decision-support）的输出质量、可读性、实用性需要人工判断，无法用断言覆盖。

调用 `cognitive_skill`:
- **skillType**: `diagnostic`
- **ruleId**: `no-console-log`
- **codePattern**: `console.log("hello")`
- **contextDescription**: `src/index.ts line 1`
- **projectDir**: `/Users/zhichao/tmp/harness-test`

- [ ] 返回结构包含 `analysis` / `suggestions`
- [ ] 诊断结果准确识别了 `console.log` 的问题
- [ ] 给出的改进建议具体、可操作
- [ ] 语言风格符合开发者预期

调用 `cognitive_skill`:
- **skillType**: `educational`
- **ruleId**: `no-magic-numbers`
- **topic**: `magic numbers`
- **experienceLevel**: `beginner`

- [ ] 教育内容适合 beginner 水平
- [ ] 包含实际代码示例
- [ ] 解释清晰，无术语堆砌

调用 `cognitive_skill`:
- **skillType**: `decision-support`
- **ruleId**: `no-console-log`
- **currentMedium**: `claude_md`
- **candidateMedia**: `["linter_warn", "linter_error", "hook"]`
- **projectPhase**: `growth`
- **teamSize**: `medium`

- [ ] 决策建议有依据（列出各介质的 trade-off）
- [ ] 推荐与项目阶段匹配（growth 阶段可适当升级介质）

---

## TC03: 错误信息优化质量评估

**原因**: 生成的错误信息的内容质量、可读性、结构完整性需要人工阅读判断。

调用 `optimize_error_message`:
- **ruleId**: `no-console-log`
- **scenario**: `src/index.ts 第 1 行出现了 console.log`
- **actualCode**: `console.log("hello")`
- **fileName**: `src/index.ts`
- **lineNumber**: 1

- [ ] 返回结构包含 `why` / `whatInstead` / `reference` 三要素
- [ ] `why` 清晰解释了为什么这条规则重要
- [ ] `whatInstead` 提供了可替代的具体写法
- [ ] `reference` 给出了可参考的文档链接或说明

再测一个复杂场景:
- **ruleId**: `no-magic-numbers`
- **scenario**: `calc 函数中 if (x > 30000) 使用了 magic number`
- **actualCode**: `if (x > 30000) return x * 2;`

- [ ] 第二个建议也能保持三要素结构
- [ ] 两条建议的模板配置不同（ID 不同）

调用 `suggest_error_improvement`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`

- [ ] 返回 `stats` 包含模板使用统计
- [ ] `suggestions` 对使用/未使用的模板给出合理建议
- [ ] 当无记录时，提示 "暂无错误信息模板使用记录"

---

## TC04: A/B 测试全生命周期（需要连续操作）

**原因**: A/B 测试涉及 start → collect → analyze 三个步骤，需要时间推进和多轮数据收集，不适合自动化。

### 4.1 启动测试

```bash
# 先确保有 evaluated 状态
cd /Users/zhichao/claude/harness
```
在 Claude Code 中调用 MCP 工具的 evaluate_rules，或直接在代码中验证。

调用 `start_ab_test`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`
- **ruleId**: `no-console-log`
- **baselineMedium**: `claude_md`
- **testMedium**: `linter_warn`
- **durationDays**: 7
- **metrics**: `["triggerCount", "fixRate", "bypassCount"]`

- [ ] 返回 `testId` 不为空
- [ ] 返回 `config` 与输入一致
- [ ] `.harness/ab-tests/` 下创建了测试目录

### 4.2 收集数据点

调用 `collect_ab_metrics`（重复 3 次，模拟不同时间点的数据）:
- **projectDir**: `/Users/zhichao/tmp/harness-test`
- **testId**: 上一步返回的 testId

| 轮次 | triggerCount | fixRate | bypassCount |
|------|-------------|---------|-------------|
| 1 | 5 | 0.8 | 1 |
| 2 | 8 | 0.6 | 2 |
| 3 | 12 | 0.4 | 4 |

- [ ] 每次调用均返回 success
- [ ] 数据文件追加了新的 dataPoint（查看 `.harness/ab-tests/<testId>/data.json`）

### 4.3 分析结果

调用 `analyze_ab_results`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`
- **testId**: 前面创建的 testId

- [ ] 返回 `dataPoints` 包含 3 条记录
- [ ] `recommendation` 为 "keep" / "revert" / "adjust" 之一
- [ ] `confidenceScore` 在 0-1 之间
- [ ] 人工判断分析结论是否合理（fixRate 下降的趋势是否被正确识别）

---

## TC05: 导出/导入 + 生成完整工作流

**原因**: 从 preset 导入 → 确认 → 生成配置的完整链路需要人工验证中间状态和最终文件内容。

```bash
# 先重置状态
rm -rf /Users/zhichao/tmp/harness-test/.harness
```

调用 `import_rules`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`
- **presetId**: `web-app-ts`

- [ ] 返回 `total: 16`
- [ ] 所有 decision 被 enrich 为完整字段（confidence > 0）

将上一步的 decisions 传递到 `confirm_decisions`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`
- **decisions**: 上一步的 events[0].text 中的 decisions

- [ ] 返回 `status: "confirmed"`
- [ ] `query_state` 显示 phase 为 confirmed

调用 `generate_config`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`
- **decisions**: []（从 state 读取）

- [ ] 返回 `files` 包含 CLAUDE.md、.claude/settings.json 等
- [ ] **人工检查 CLAUDE.md 内容**: 格式正确、规则分类清晰
- [ ] **人工检查 settings.json**: 合法 JSON、包含 formatOnSave

---

## TC06: 生成文件语法正确性验证

**原因**: 文件内容「合法」不仅仅是 JSON parse 不抛异常，还需要被下游工具真正接受。这些需要人工检查或用真实工具验证。

### 6.1 ESLint Flat Config

```bash
cd /Users/zhichao/tmp/harness-test

# 先确保 eslint.config.js 已生成
# 如果 generate_config 返回了 eslint.config.js:
npm install -D eslint @eslint/js typescript-eslint 2>/dev/null || true
npx eslint --no-eslintrc -c eslint.config.js src/index.ts
```

- [ ] ESLint 能正常加载 eslint.config.js（无 config 解析错误）
- [ ] 检查结果正确识别了 console.log / debugger
- [ ] ESLint 版本兼容性（>=9.0 支持 flat config）

### 6.2 CI YAML

```bash
# 如果有生成 .github/workflows/ci.yml
pip install yamllint 2>/dev/null && yamllint /Users/zhichao/tmp/harness-test/.github/workflows/ci.yml 2>/dev/null
# 或在线 YAML validator
```

- [ ] YAML 语法正确
- [ ] `on` / `jobs` 结构正确
- [ ] ESLint 命令使用 `--config eslint.config.js`

### 6.3 Husky Hook

```bash
# 查看 hook 内容
cat /Users/zhichao/tmp/harness-test/.husky/pre-commit
```

- [ ] hook 以 `#!/bin/sh` 开头
- [ ] hook 文件有执行权限（ls -la 可见 `-rwxr-xr-x`）
- [ ] hook 逻辑合理（调用了 lint-staged 或 eslint）

---

## TC07: 回滚正确性（含 managed files 清理）

**原因**: 回滚不仅要恢复备份文件，还要清理「备份中不存在但属于 managed files」的文件。这个逻辑需要人工观察确认。

```bash
cd /Users/zhichao/tmp/harness-test

# 1. 第一次 init_harness（生成文件）
# 2. 把文件写入磁盘
# 3. 手动创建 managed files 列表之外的文件
# 4. 第二次 init_harness（创建备份 + 覆盖）
# 5. 修改 CLAUDE.md
# 6. 执行 rollback
```

详细步骤:

```bash
# Step 1: init_harness in Claude Code
# ... 获取 files 列表

# Step 2: 写文件到磁盘
# ... 在 Claude Code 中自动写入

# Step 3: 在备份后新增一个 managed file
touch /Users/zhichao/tmp/harness-test/.husky/pre-commit
# 注意: 此文件在第一次 init 时可能没有生成（取决于规则）

# Step 4: 第二次 init_harness（会备份现有文件）
# Step 5: 修改 CLAUDE.md
echo "MANUALLY MODIFIED" >> /Users/zhichao/tmp/harness-test/CLAUDE.md

# Step 6: rollback
```

- [ ] 返回 `status: "success"`
- [ ] `restored` 包含被恢复的文件
- [ ] `cleaned` 中包含了「备份中没有但属于 managed files」的文件被清理
- [ ] CLAUDE.md 内容回到备份版本（不再含 "MANUALLY MODIFIED"）

```bash
# 测试指定 backupId 恢复
```
- [ ] 指定备份 ID 也能正确恢复

---

## TC08: 大项目扫描 —— 性能主观评估

**原因**: 扫描性能（尤其 scanDepth=full）的"快"与"慢"是主观体验，难以用自动化测试覆盖。

找一个至少有 50+ 源文件的项目:

```bash
# 在 harness 自身项目上做扫描
```

调用 `scan_codebase`:
- **projectDir**: `/Users/zhichao/tmp/harness-test`（确保此目录有 50+ 文件，或用真实项目）
- **techStack**: `["typescript"]`
- **projectPhase**: `growth`
- **teamSize**: `medium`
- **scanDepth**: `quick`

- [ ] quick 扫描在 3 秒内返回结果
- [ ] `filesScanned` 数量合理

```bash
# 第二次扫描，开启 cache
```
- [ ] 带 cache 的扫描比首次快（返回时间减少 50%+）
- [ ] `.harness/scan-cache.json` 文件已创建

```bash
# 扫一个纯空目录
rm -rf /Users/zhichao/tmp/harness-test
mkdir -p /Users/zhichao/tmp/harness-test
```

- [ ] 空目录扫描不崩溃，返回空结果

---

## TC09: 重复操作与状态一致性

**原因**: 连续重复调用、跨项目操作等涉及状态机健壮性，需要在真实文件系统上验证。

```bash
# 准备两个测试项目
rm -rf /Users/zhichao/tmp/harness-test /tmp/harness-test-2
mkdir -p /Users/zhichao/tmp/harness-test /tmp/harness-test-2
```

1. 在项目 A 执行 `evaluate_rules(projectPhase=prototype, teamSize=solo)`
2. 在项目 B 执行 `evaluate_rules(projectPhase=mature, teamSize=large)`
3. 分别 `query_state`

- [ ] 项目 A 的 state 与项目 B 的 state 完全隔离
- [ ] 项目 A 的 hook/ci 建议比项目 B 少

4. 在项目 A 连续调用 2 次 `evaluate_rules`（不同参数）
- [ ] 第二次调用覆盖了第一次的结果
- [ ] `query_state` 反映的是最新一次评估

5. 在项目 A 已经 confirmed 的情况下，再次调用 `evaluate_rules`
- [ ] 除非调用 `reset_state`，否则 confirm_decisions 可以正确被重新 evaluate

6. `generate_config` → 不写磁盘 → `reset_state` → 再 `generate_config`
- [ ] 第二次 `generate_config` 返回 NO_DECISIONS 错误

---

## TC10: 异常场景人工验证

**原因**: 异常场景的「错误信息可读性」「是否泄露内部实现细节」需要人工判断。

### 10.1 不存在的工具名

发送未知工具名:

- [ ] 返回标准的 `HarnessError` 结构（code/message/detail/recoverable）
- [ ] 错误信息对用户友好，不包含内部调用栈
- [ ] `isError: true`

### 10.2 特殊路径

```bash
# 项目路径包含空格
mkdir -p "/Users/zhichao/tmp/harness test space"
```

- [ ] 路径含空格也能正常工作
- [ ] 路径含中文也能正常工作（如果系统允许）

### 10.3 并发调用

```bash
# 快速连续调用两次 evaluate_rules（不同参数）
```

- [ ] 两次调用都正确返回，没有串数据
- [ ] 最终 state 是最新一次评估的结果

---

## TC11: 一键初始化 —— 完整性验证

**原因**: `init_harness` 做了 evaluate → confirm → generate → validate 多个步骤，最终生成内容和验证结果需要人工确认。

```bash
cd /Users/zhichao/tmp/harness-test
# 准备好 package.json + 代码文件后
```

调用 `init_harness`:
- **projectPhase**: `growth`
- **teamSize**: `medium`
- **techStack**: `["typescript"]`

- [ ] 返回 `files` 列表完整（CLAUDE.md、.claude/settings.json、.gitignore、eslint.config.js、.husky/、.github/workflows/ci.yml）
- [ ] `validation` 中 `status` 为 "pass"（如果文件已写入磁盘）
- [ ] `summary.backupDir` 合理

```bash
# 删除 CLAUDE.md
rm /Users/zhichao/tmp/harness-test/CLAUDE.md

# 调用 validate_setup
```

- [ ] 验证结果 `status` 为 "fail"
- [ ] findings 中包含 "CLAUDE.md" 缺失的错误

```bash
# 恢复后再次 validate_setup
```
- [ ] `status` 回到 "pass"

```bash
# 测试 dryRun
```
- [ ] dryRun 模式不写磁盘
- [ ] dryRun 返回的 decisions 数量 > 0

---

## TC12: npm publish 流水线（可选，需要 npm 账号）

**原因**: npm publish 需要真实账号和 token，且涉及 `.npmignore` 效果验证，无法自动化。

```bash
cd /Users/zhichao/claude/harness/mcp-server

# 检查 npmignore
head -20 .npmignore
```

- [ ] `.npmignore` 排除了 `src/`、测试文件、`tsconfig.json`
- [ ] package.json 的 `files` 字段与 `.npmignore` 一致

```bash
# 本地 pack 测试
npm pack --dry-run
```

- [ ] pack 结果**不包含** `.test.ts`、`src/` 源文件
- [ ] pack 结果**包含** `dist/`、`eslint.config.js`、`rules.json`

```bash
# 实际 publish（可选）
# npm publish --dry-run
```

- [ ] npm publish 流程正常执行
- [ ] 已设置 `--access public`（scoped package）
- [ ] 版本号正确递增

---

## 检查清单汇总

| # | 测试项 | 人工判断点 | 结果 |
|---|--------|-----------|------|
| TC01 | assess_suitability | 评估结果合理性、full vs quick 差异 | □ 通过 |
| TC02 | cognitive_skill | 诊断/教育/决策输出质量 | □ 通过 |
| TC03 | 错误消息优化 | why/whatInstead/reference 三要素完整度 | □ 通过 |
| TC04 | A/B 测试全生命周期 | 多轮数据收集 + 分析结论合理性 | □ 通过 |
| TC05 | preset 导入 + 生成 | 中间状态正确性 + 最终文件内容 | □ 通过 |
| TC06 | 语法正确性 | ESLint 加载、YAML 校验、hook 可执行 | □ 通过 |
| TC07 | 回滚完整性 | managed files 清理、备份恢复正确 | □ 通过 |
| TC08 | 扫描性能 | quick/cache 速度主观评价 | □ 通过 |
| TC09 | 多项目状态隔离 | 独立状态、覆盖、重置 | □ 通过 |
| TC10 | 异常场景 | 错误可读性、路径兼容、并发 | □ 通过 |
| TC11 | init_harness 完整性 | 文件内容、验证结果、dryRun 行为 | □ 通过 |
| TC12 | npm publish（可选） | 包内容正确、publish 流程 | □ 通过 |

---

*Test plan generated for Harness Automation System v1.0.7 — 2026-04-27*
