# 全面修复计划 — OpenAPI 规范 vs 实现差异 (v5)

**计划日期**: 2026-04-25 14:55
**依据**: `docs/verifications/gap-analysis-20260425-1455.md`
**验证方法**: 逐条对照 `mcp-server/src/` 当前源代码确认每项差异的真实状态

---

## 0. 前提：交叉验证结果

编制本计划前，逐条对照 2026-04-25 14:55 时刻的当前代码验证了全部 5 项差异，确认以下状态：

| 报告发现的缺口 | 当前代码实际状态 | 本计划处理 |
|---|---|---|
| N-01: CognitiveSkillInput 无 Zod Schema | `types.ts` 中不存在 `CognitiveSkillInputSchema`；`index.ts:239-256` 使用内联 JSON Schema；`index.ts:1203-1216` 使用 `args as` 类型断言 | 纳入 P1-1 |
| N-02: 10 个工具使用 `args as X` 而非 Zod.parse() | 确认 10 处：query_state:423, reset_state:488, list_rule_exports:1128, start_ab_test:1143, collect_ab_metrics:1163, analyze_ab_results:1182, assess_suitability:1190, cognitive_skill:1203, suggest_error_improvement:1233, optimize_error_message:1258 | 纳入 P1-2 |
| N-03: RollbackOutput 类型与运行时不匹配 | `types.ts:370-373`: `failed: string[]`, `errors: string[]`, `cleaned?: string[]` — 运行时实际发送 `null` | 纳入 P1-3 |
| N-04: RuleConflict.type 未约束枚举 | `types.ts:132`: `type: string` — OAS 声明 `enum: [behavioral, overlap]`，代码也只用这两个值 | 纳入 P2-1 |
| N-05: cognitive_skill 错误响应缺 pretty-print | `index.ts:1224`: `JSON.stringify({...})` 缺 `null, 2` 参数 | 纳入 P2-2 |

> 历史报告 (`1242`/`1328`/`1416`/`1430`) 的 59 项差异已全部关闭或在本计划追踪。

---

## 1. P1 — 类型安全与输入校验

### P1-1: CognitiveSkillInput 缺少 Zod Schema（新发现）

| 维度 | 值 |
|------|-----|
| **缺口** | `CognitiveSkillInput` 在 `types.ts` 中无 Zod Schema；handler 使用 `args as` 类型断言，无运行时校验 |
| **来源** | 1455 N-01 |
| **历史** | **新发现** |

**当前状态**:

- `types.ts` 末尾 (line 437): 无 `CognitiveSkillInputSchema` 导出
- `index.ts:239-256`: 注册工具时使用内联 JSON Schema（非从 Zod 派生）
- `index.ts:1203-1216`: handler 中 `args as { skillType, ruleId, ... }` 直接类型断言
- `cognitive_layer/orchestrator.ts:16-29`: 已有 `CognitiveRequest` 接口定义

**改动**: 在 `types.ts` 添加 Zod Schema：

```typescript
export const CognitiveSkillInputSchema = z.object({
  skillType: z.enum(["diagnostic", "educational", "decision-support"]).describe("技能类型"),
  ruleId: z.string().describe("规则 ID 或名称"),
  projectDir: z.string().optional().describe("项目目录（可选，用于加载规则）"),
  codePattern: z.string().optional().describe("代码模式（diagnostic 使用）"),
  contextDescription: z.string().optional().describe("上下文描述（diagnostic 使用）"),
  topic: z.string().optional().describe("主题（educational 使用）"),
  experienceLevel: z.enum(["beginner", "intermediate", "advanced"]).optional().describe("经验水平（educational 使用）"),
  currentMedium: z.string().optional().describe("当前介质（decision-support 使用）"),
  candidateMedia: z.array(z.string()).optional().describe("候选介质列表（decision-support 使用）"),
  projectPhase: z.string().optional().describe("项目阶段（decision-support 使用）"),
  teamSize: z.string().optional().describe("团队规模（decision-support 使用）"),
  techStack: z.array(z.string()).optional().describe("技术栈（decision-support 使用）"),
});

export type CognitiveSkillInput = z.infer<typeof CognitiveSkillInputSchema>;
```

同步更新 `index.ts`:
1. `index.ts:239` 注册的 `inputSchema` 改为 `z(CognitiveSkillInputSchema)`（从 types.ts 导入）
2. `index.ts:1203-1216` handler 中：
   ```typescript
   const cogInput = CognitiveSkillInputSchema.parse(args);
   const result: CognitiveResponse = processCognitiveRequest(cogInput);
   ```

**文件改动**: `types.ts` + `index.ts` 共 2 个文件

**验证方法**:
- 调用 `cognitive_skill` 传无效 `skillType`（如 `"foo"`）→ Zod 返回校验错误
- 调用 `cognitive_skill` 不传 `ruleId` → Zod 返回校验错误
- 正常调用三种 `skillType` 均正常工作

---

### P1-2: 10 个工具缺少 Zod 运行时输入校验（新发现）

| 维度 | 值 |
|------|-----|
| **缺口** | 10 个工具 handler 使用 `args as X` 而非 `Zod.parse()`，无运行时输入校验 |
| **来源** | 1455 N-02 |
| **历史** | **新发现** |

**当前状态** — 以下 10 个工具均缺少 Zod Schema：

| 工具 | 文件:行 | 当前处理 |
|------|---------|---------|
| `query_state` | index.ts:423 | `args as { projectDir: string }` |
| `reset_state` | index.ts:488 | `args as { projectDir: string }` |
| `list_rule_exports` | index.ts:1128 | `args as { projectDir: string }` |
| `start_ab_test` | index.ts:1143 | `args as { projectDir, ruleId, baselineMedium, ... }` |
| `collect_ab_metrics` | index.ts:1163 | `args as { projectDir, testId, ... }` |
| `analyze_ab_results` | index.ts:1182 | `args as { projectDir, testId? }` |
| `assess_suitability` | index.ts:1190 | `args as { projectDir, techStack?, analysisDepth? }` |
| `cognitive_skill` | index.ts:1203 | `args as { skillType, ruleId, ... }`（见 P1-1） |
| `suggest_error_improvement` | index.ts:1233 | `args as { projectDir }` |
| `optimize_error_message` | index.ts:1258 | `args as { projectDir, ruleId?, ... }` |

**改动方案**: 为每个工具在 `types.ts` 中创建 Zod Schema，并在 `index.ts` 中替换注册和 handler。

由于 10 个工具较多，分两步实施：

**Step 1** — 在 `types.ts` 末尾添加 10 个 Schema（P1-1 已含 cognitive_skill）：
```typescript
// 简单工具（仅 projectDir）
export const QueryStateInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
});
export const ResetStateInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
});
export const ListRuleExportsInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
});
export const SuggestErrorImprovementInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
});

// 中等工具（2-4 参数）
export const AnalyzeABResultsInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
  testId: z.string().optional().describe("可选，不指定则分析所有活跃测试"),
});

export const AssessSuitabilityInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
  techStack: z.array(z.string()).optional().describe("技术栈（可选）"),
  analysisDepth: z.enum(["quick", "full"]).optional().default("full").describe("分析深度"),
});

// 复杂工具（多参数）
export const StartABTestInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
  ruleId: z.string().describe("规则 ID"),
  baselineMedium: z.enum([...RuleMediumEnumValues]),
  testMedium: z.enum([...RuleMediumEnumValues]),
  durationDays: z.number().optional().default(14),
  metrics: z.array(z.string()).optional().default(["triggerCount", "fixRate", "bypassCount"]),
});

export const CollectABMetricsInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
  testId: z.string().describe("测试 ID"),
  triggerCount: z.number().describe("触发次数"),
  fixRate: z.number().describe("修复率（0-1）"),
  bypassCount: z.number().describe("绕过次数"),
  userFeedback: z.string().optional().describe("用户反馈"),
});

export const OptimizeErrorMessageInputSchema = z.object({
  projectDir: z.string().describe("项目目录绝对路径"),
  ruleId: z.string().optional(),
  ruleName: z.string().optional(),
  scenario: z.string().optional(),
  actualCode: z.string().optional(),
  fileName: z.string().optional(),
  lineNumber: z.number().optional(),
  rateAfter: z.boolean().optional(),
});
```

**Step 2** — 在 `index.ts` 中：
1. 注册改用 `z(SchemaName)`（10 处）
2. Handler 改用 `SchemaName.parse(args)`（10 处）

**验证方法**:
- 每个工具的有效调用正常工作
- `query_state` 不传 `projectDir` → Zod 校验错误
- `assess_suitability` 传 `analysisDepth: "invalid"` → Zod 校验错误
- `start_ab_test` 传无效 `baselineMedium` → Zod 校验错误

---

### P1-3: RollbackOutput 类型定义与运行时不匹配（反复出现）

| 维度 | 值 |
|------|-----|
| **缺口** | `types.ts` 中 `RollbackOutput.failed/errors/cleaned` 类型为 `string[]`，但运行时实际发送 `null`（已修复 P8-8） |
| **来源** | 1455 N-03 |
| **历史** | **反复出现**（P8-8 修复了运行时行为，但类型定义未同步更新） |

**当前状态**:

- `types.ts:370`: `failed: string[]` — 应为 `string[] | null`
- `types.ts:371`: `errors: string[]` — 应为 `string[] | null`
- `types.ts:373`: `cleaned?: string[]` — 应为 `string[] | null`（可选+可空）
- `index.ts:866-872`: 运行时正确发送 `null`

**改动**: `types.ts:367-374`:
```typescript
export interface RollbackOutput {
  status: "success" | "partial" | "failed";
  restored: string[];
  failed: string[] | null;
  errors: string[] | null;
  backupId: string;
  cleaned?: string[] | null;
}
```

**验证方法**: `npm run build` 通过，无类型错误；任何引用 `RollbackOutput` 的代码正确处理 `null`

---

## 2. P2 — 细节优化

### P2-1: RuleConflict.type 枚举约束（反复出现）

| 维度 | 值 |
|------|-----|
| **缺口** | `types.ts` 中 `RuleConflict.type` 为 `string`，但 OAS 声明 `enum: [behavioral, overlap]`，代码也只用这两个值 |
| **来源** | 1455 N-04 |
| **历史** | **反复出现**（P7-11 修复了 OAS 但未更新 types.ts） |

**当前状态**:

- `types.ts:132`: `type: string`
- `engine.ts:86-128` CONFLICT_MATRIX: 只使用 `"behavioral"` 和 `"overlap"`
- OAS (`openapi.yaml:1483-1485`): `type: string; enum: [behavioral, overlap]`

**改动**: `types.ts:129-135`:
```typescript
export interface RuleConflict {
  ruleA: string;
  ruleB: string;
  type: "behavioral" | "overlap";
  description: string;
  resolution: string;
}
```

**验证方法**: `npm run build` 通过，无类型错误；`engine.ts` 中所有 `type` 赋值兼容新类型

---

### P2-2: cognitive_skill 错误响应 JSON pretty-print（新发现）

| 维度 | 值 |
|------|-----|
| **缺口** | `cognitive_skill` handler 的 catch 分支中 `JSON.stringify` 缺少 `null, 2` 格式化参数 |
| **来源** | 1455 N-05 |
| **历史** | **新发现** |

**当前状态**:

- `index.ts:1224`:
  ```typescript
  text: JSON.stringify({
    error: { code: "UNKNOWN_ERROR", message: String(e), recoverable: true } satisfies HarnessError,
  })
  ```
- 其他所有错误返回（`confirm_decisions` 等 11 处）均使用 `JSON.stringify({...}, null, 2)`

**改动**: `index.ts:1224`:
```typescript
text: JSON.stringify({
  error: { code: "UNKNOWN_ERROR", message: String(e), recoverable: true } satisfies HarnessError,
}, null, 2),
```

**验证方法**: `cognitive_skill` 传不存在的 `ruleId` → 返回 JSON 为格式化换行的错误对象

---

## 3. 汇总

| 分级 | ID | 描述 | 发现类型 | 修改对象 | 文件数 |
|------|----|------|---------|---------|--------|
| **P1** | P1-1 | CognitiveSkillInput 缺少 Zod Schema | **新发现** | types.ts + index.ts | 2 |
| **P1** | P1-2 | 10 个工具缺少 Zod 运行时校验 | **新发现** | types.ts + index.ts | 2 |
| **P1** | P1-3 | RollbackOutput 类型不匹配 | **反复出现** | types.ts | 1 |
| **P2** | P2-1 | RuleConflict.type 枚举未约束 | **反复出现** | types.ts | 1 |
| **P2** | P2-2 | cognitive_skill 错误缺 pretty-print | **新发现** | index.ts | 1 |

**统计**: 新发现 3 项 / 反复出现 2 项 = **5 项**
**代码改动**: 3 个文件（types.ts, index.ts）

### 与历次报告对照

| 报告 | 缺口数 | 修复数 | 剩余 |
|------|--------|--------|------|
| 1242 (Design vs 代码) | 13 | 13 | 0 |
| 1328 (Design vs 代码 二遍) | 21 | 21 | 0 |
| 1416 (OpenAPI vs 代码) | 11 | 11 | 0 |
| 1430 (OpenAPI vs 代码 二遍) | 14 | 14 | 0 |
| **1455 (OpenAPI vs 代码 三遍)** | **5** | **本计划** | **5** |

---

## 4. 验证清单

修复完成后，按以下顺序逐项验证：

### 编译 & 基础校验
- [ ] `npm run build` 通过，无 TypeScript 编译错误
- [ ] `npm run lint` 输出 0 errors
- [ ] `npm test` 全部测试通过（现有 304+ 测试不受影响）
- [ ] OpenAPI spec 通过语法校验：`npx @redocly/cli lint docs/api/openapi.yaml`

### P1 验证
- [ ] **P1-1 CognitiveSkillInput Zod**: `cognitive_skill` 传无效 `skillType` → Zod 校验错误（非静默接受）
- [ ] **P1-1 CognitiveSkillInput 正常**: 三种 `skillType`（diagnostic/educational/decision-support）均正常返回
- [ ] **P1-2 query_state Zod**: `query_state` 不传 `projectDir` → Zod 校验错误
- [ ] **P1-2 start_ab_test Zod**: `start_ab_test` 传无效 `baselineMedium` → Zod 校验错误
- [ ] **P1-2 assess_suitability Zod**: `assess_suitability` 传 `analysisDepth: "invalid"` → Zod 校验错误
- [ ] **P1-2 剩余工具**: 其余 7 个工具的调用不受影响
- [ ] **P1-3 RollbackOutput 类型**: 编译后引用 `RollbackOutput.failed` 的代码正确处理 `null`

### P2 验证
- [ ] **P2-1 RuleConflict.type**: 编译通过，`engine.ts` 中 CONFLICT_MATRIX 赋值兼容新类型
- [ ] **P2-2 cognitive_skill pretty-print**: 错误响应 JSON 含换行/缩进（`null, 2` 生效）

### 回归验证
- [ ] 核心流程：`evaluate_rules → confirm_decisions → generate_config → validate_setup`
- [ ] 快捷路径：`init_harness`（含全部参数）
- [ ] A/B 测试：`start_ab_test → collect_ab_metrics → analyze_ab_results`
- [ ] 扫描路径：`scan_codebase → query_state`
- [ ] 回滚路径：修改文件 → `rollback` → 文件恢复（验证 failed=null）
- [ ] 认知层：三种 `skillType` 调用均正常
- [ ] 规则管理：`list_rule_presets → import_rules → export_rules → list_rule_exports`
