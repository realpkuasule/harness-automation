# Full Fix Plan: OpenAPI Spec vs Implementation

**Date**: 2026-04-25 19:30
**Based on**: `docs/verifications/gap-analysis-20260425-1930.md`
**Verification method**: Independent code review — cross-referenced spec against all 22 tool handlers, Zod schemas, interface definitions, and output types

---

## Historical Context

| Report | Date | New Issues | Resolved | Outcome |
|--------|------|-----------|----------|---------|
| 1905 | Apr 25 19:05 | 8 | — | P12 fix plan created |
| **1930** | **Apr 25 19:30** | **0** | **8 (P12)** | **All gaps closed** |

All 8 issues from the 1905 gap analysis were fixed in P12. This report independently verifies the fix results and confirms no new gaps exist.

---

## Independent Verification Results

### P12 Fix Verification (8/8 Confirmed)

Each item was verified by reading both the spec and implementation code directly:

| ID | Issue | Spec Check | Impl Check | Verdict |
|----|-------|-----------|------------|---------|
| P12-1 | `optimize_error_message.projectDir` required | `required: []` (line 938) | `z.string().optional()` (types.ts:497) | ✅ Fixed |
| P12-2 | `start_ab_test.endTime` optional | `description: "仅 status=started 时存在"` (line 725) | `endTime?: string` (manager.ts:20) | ✅ Fixed |
| P12-3 | `init_harness.errors` always emitted | Property always listed (line 255-258) | `errors` no ternary (index.ts:591) | ✅ Fixed |
| P12-4 | `generate_config.decisions` fallback | Description includes "回退到 state.json" (line 1170-1172) | Fallback to `state.engineOutput.decisions` (index.ts:260-262) | ✅ Fixed |
| P12-5 | `query_state` conditional fields | Description notes conditional emission (line 115-116) | 7 conditional `if` blocks (index.ts:371-397) | ✅ Fixed |
| P12-6 | `optimize_error_message.stats` conditional | Description notes condition (line 970-971) | `stats` only when `projectDir && rateAfter !== false` (index.ts:1182-1188) | ✅ Fixed |
| P12-7 | `suggestedCommands` conditional | Description "仅在检测到缺失依赖时返回" (line 2289) | Only added when `depMerge.missing.length > 0` (index.ts:554-556) | ✅ Fixed |
| P12-8 | RuleDefinition min/max constraints | `minimum: 1, maximum: 5` on 3 fields (lines 1086-1097) | Types are `number` with runtime clamping via `Math.min/Math.max` in engine.ts | ✅ Fixed |

### Full Schema Audit

All 22 tool input schemas (Zod ↔ spec) and output types (interface ↔ spec) verified:

| Category | Items Checked | Result |
|----------|--------------|--------|
| Tool input schemas | 22 Zod schemas vs spec requestBody | ✅ All match |
| Tool response types | 22 handler return values vs spec responses | ✅ All match |
| Enum values | 10 enums, 55 values total | ✅ All match |
| Component schemas | All `$ref` targets resolve to matching types | ✅ All match |

### Key Checks

1. **Tool count**: 22 in spec, 22 in index.ts `switch` — match ✅
2. **`init_harness` dryRun response**: Returns sparse subset of normal response — compatible (no `required` on any field) ✅
3. **`cognitive_skill` response types**: `DiagnosticResult`, `EducationalResult`, `DecisionSupportResult` — all 3 match spec oneOf variants ✅
4. **`CollectABMetricsInput`**: 5 required fields in spec — all `z.string()`/`z.number()` without `.optional()` ✅
5. **`ConfigFileResult.action`**: 5 enum values `[created, overwritten, skipped, merged, dry_run]` — match ✅
6. **`ErrorCode`**: 13 values including `NO_DECISIONS`, `UNKNOWN_TOOL` — match ✅
7. **`SuitabilityWarning.type`**: 3 values `[prototype, script, overhead]` — match ✅
8. **`ABTestResult.recommendation`**: 3 values `[keep, revert, adjust]` — match ✅

---

## Priority Assessment

| Priority | Meaning | Count |
|----------|---------|-------|
| **P0** | Blocking — client cannot parse response | 0 |
| **P1** | Contract compatibility — client expectations may break | 0 |
| **P2** | Documentation — behavior is correct but undocumented | 0 |
| **P3** | Consistency — low impact, cosmetic | 0 |

**No items require fixes.**

---

## Verification Checklist

After applying all fixes (none needed for 1930), run through this checklist to confirm alignment:

### Tool Coverage
- [ ] `grep -c 'name:' docs/api/openapi.yaml` = 22 tools registered in ListToolsRequestSchema
- [ ] Each tool name in spec matches one `case` in index.ts switch statement

### Enum Alignment
- [ ] `RuleMedium`: 11 values in spec == 11 values in types.ts
- [ ] `TechStack`: 6 values matching
- [ ] `ProjectPhase`: 4 values matching
- [ ] `TeamSize`: 4 values matching
- [ ] `HarnessStatus`: 4 + null matching
- [ ] `ErrorCode`: 13 values matching

### P12 Regression (verify no re-introduction)
- [ ] `yq` or grep confirms `optimize_error_message` required list is `[]`
- [ ] `start_ab_test` spec response has `endTime` description noting conditional presence
- [ ] `init_harness` response always includes `errors` array (code: line 591)
- [ ] `generate_config` spec documents decisions fallback behavior
- [ ] `query_state` spec description includes conditional-field note
- [ ] `optimize_error_message` spec response has `stats` conditional description
- [ ] `suggestedCommands` spec description notes conditional presence
- [ ] `cost`/`feedbackSpeed`/`frequency` spec entries have `minimum: 1, maximum: 5`

### Build & Test
- [ ] `npm run build` — successful TypeScript compilation
- [ ] `npm run test` — all existing tests pass
- [ ] `npm run lint` — no new errors

---

## Conclusion

**0 items require fixing.** All 8 issues from the previous gap analysis (1905) have been resolved in P12 and independently verified. The implementation and OpenAPI spec are fully aligned across all 22 tools with no contract-breaking gaps.
