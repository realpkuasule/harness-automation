# Full Fix Plan: OpenAPI Spec vs Implementation Gaps

**Source**: `docs/verifications/gap-analysis-20260426-1321.md`
**Date**: 2026-04-26 13:21
**Previous plans**:
- `full-fix-20260425-2026.md` — 2 items, both applied ✅
- `full-fix-20260425-2209.md` — 6 items (P14-1~P14-6), all applied ✅

---

## Previous Plan Verification

Before adding new items, verify that all 6 fixes from the previous plan (`full-fix-20260425-2209.md`) are still correctly applied:

| Item | Fix | Expected State | Actual Status |
|------|-----|---------------|---------------|
| P14-1 (2209)P1-A | `DepCheckResult.outdated` → `Array<{name,current,wanted}>` | `deps.ts:21` should be object array | ✅ `Array<{ name: string; current: string; wanted: string }>` |
| P14-2 (2209)P2-A | `checkFiles` add `default:` | `openapi.yaml:~1317` should have default list | ✅ 9-file default list present |
| P14-3 (2209)P2-B | Document `ruleName` fallback | `openapi.yaml:927` should mention ruleName | ✅ "不提供 ruleId、scenario 和 ruleName 时 suggestions 为空数组" |
| P14-4 (2209)P3-A | Document dryRun summary fields | `openapi.yaml:224` should list absent fields | ✅ "dryRun 模式下 summary 仅包含 files 和 decisions" |
| P14-5 (2209)P3-B | Align `reset_state` description | `index.ts:119` should mention phase reset | ✅ "将状态机的 phase 重置为 null，用于重新开始流程" |
| P14-6 (2209)P3-C | Document import_rules round-trip loss | `openapi.yaml:496` should mention optional field loss | ✅ "round-trip 数据截断" mentioned |

**No regressions detected.** All 6 items from the previous plan are stable.

---

## Gap Verification (re-checked against source code)

### Analysis report claim: 0 gaps, 100% alignment

**Verification result**: ✅ CONFIRMED via independent code check

The following dimensions were verified directly against source code:

| Dimension | Verification Method | Result |
|-----------|-------------------|--------|
| Tool registration | Regex scan of `index.ts:100-212` vs `openapi.yaml` const definitions | ✅ 22/22 tools matched |
| Input schemas | Cross-reference Zod schemas (`types.ts`) with OpenAPI `properties` | ✅ All fields match |
| Output schemas | Cross-reference TypeScript interfaces with OpenAPI component schemas | ✅ 60/60 schemas match |
| Error codes | Comparison of `types.ts:48-61` with `openapi.yaml` ErrorCode enum | ✅ 13/13 codes match |
| Error handling | Behavioral check: error code usage in `index.ts` handlers | ✅ All consistent |
| P14 fixes | Direct file reads of `deps.ts:21`, `openapi.yaml:224/496/927/1317`, `index.ts:119`, `types.ts:506` | ✅ All 6 stable |
| P13-14 fix | Verify `evaluatedAt` present in `types.ts:232`, `openapi.yaml:2253`, `index.ts:365` | ✅ Stable |
| P15 changes | Check API surface impact | ✅ Zero API impact (packaging/CI/docs only) |

---

## Priority Classification

| Grade | Criteria | Count |
|-------|----------|-------|
| **P0** | Observable behavioral error, data corruption, or crash | **0** |
| **P1** | Schema contract violation (code produces different shape than declared) | **0** |
| **P2** | Spec documentation gap (schema/property missing from spec) | **0** |

**Total open gaps: 0**

---

## Fix Items

**No fix items required.** All known gaps from previous reports have been resolved and verified stable across 3 consecutive analysis cycles:

| Report | Date | Gaps Found | Status |
|--------|------|-----------|--------|
| `gap-analysis-20260425-2209.md` | 2026-04-25 | 5 gaps | ✅ All fixed (P14) |
| `gap-analysis-20260426-0920.md` | 2026-04-26 | 0 gaps | ✅ Confirmed |
| `gap-analysis-20260426-1321.md` | 2026-04-26 | 0 gaps | ✅ Confirmed (this report) |

---

## Historical Gap Summary

For reference, all gaps that have ever been identified and fixed:

| Gap | First Seen | Fixed In | Category | Current State |
|-----|-----------|---------|----------|---------------|
| `OptimizeErrorMessageInputSchema.projectDir` required vs optional | 2026-04-25 (2026 report) | P13-13 | Schema mismatch | ✅ `.optional()` at `types.ts:506` |
| `HarnessState.evaluatedAt` missing from spec | 2026-04-25 (2026 report) | P13-14 | Spec omission | ✅ Present at `openapi.yaml:2253` |
| `DepCheckResult.outdated` string[] vs object[] | 2026-04-25 (2209 report) | P14-1 | Schema mismatch | ✅ Object array at `deps.ts:21` |
| `checkFiles` missing `default` | 2026-04-25 (2209 report) | P14-2 | Spec omission | ✅ Default list at `openapi.yaml:1317` |
| `ruleName` fallback undocumented | 2026-04-25 (2209 report) | P14-3 | Doc gap | ✅ Described at `openapi.yaml:927` |
| dryRun summary field behavior undocumented | 2026-04-25 (2209 report) | P14-4 | Doc gap | ✅ Noted at `openapi.yaml:224` |
| `reset_state` description misaligned | 2026-04-25 (2209 report) | P14-5 | Cosmetic | ✅ Aligned at `index.ts:119` |
| `import_rules` round-trip data loss undocumented | 2026-04-25 (2209 report) | P14-6 | Doc gap | ✅ Noted at `openapi.yaml:496` |

**No item has recurred.** Each gap was fixed once and remained stable.

---

## Verification Checklist

Since there are 0 open gaps, this checklist serves as a regression safety check:

### Code Integrity
- [x] `npm run build` — TypeScript compiles without errors
- [x] `npx vitest run` — all 320 tests pass
- [x] `npm run lint` — ESLint passes (0 errors)

### P14 Fix Stability (no regressions)
- [x] `deps.ts:21` — `DepCheckResult.outdated` is `Array<{name, current, wanted}>`
- [x] `openapi.yaml:1317` — `checkFiles` has `default:` declaration matching `MANAGED_FILES`
- [x] `openapi.yaml:927` — description mentions `ruleName` as third matching path
- [x] `openapi.yaml:224` — dryRun description lists absent summary fields
- [x] `index.ts:119` — `reset_state` description mentions phase reset
- [x] `openapi.yaml:496` — `import_rules` description mentions round-trip data loss

### Historical Fix Stability
- [x] `types.ts:506` — `projectDir` is `.optional()`
- [x] `openapi.yaml:2253` — `HarnessState` has `evaluatedAt` field
- [x] `index.ts:365` — evaluatedAt used in query_state handler

### No Regressions from P15 Changes
- [x] P15 changes are packaging/CI/docs only — zero API impact
- [x] `dist/` no longer contains test files (P15-1)
- [x] `npm pack --dry-run` shows clean package (P15-2, P15-3)

---

## Summary

| Priority | Count | Type |
|----------|-------|------|
| P0 (behavioral errors) | 0 | — |
| P1 (schema violations) | 0 | — |
| P2 (spec documentation gaps) | 0 | — |
| **Total** | **0** | **No fixes needed** |

**Current state**: Implementation and OpenAPI spec are fully synchronized. All 8 historical gaps have been resolved with no regressions. No new gaps detected.
