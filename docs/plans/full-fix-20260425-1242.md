# Harness 全面修复计划

**计划日期**: 2026-04-25 12:42
**来源**: `docs/gap-analysis/gap-analysis-20260425-1242.md`（设计 v3.0 vs 实现对比）
**验证方法**: 每条差异均经过代码确认，标注报告与实际的偏差

---

## 验证说明

编制本计划前，逐条对照代码验证了差异分析报告的全部 13 项缺口，发现以下偏差需修正：

| 报告原说 | 实际状态 | 修正 |
|---------|---------|------|
| `dryRun` 参数缺失 | `EvaluateRulesInputSchema`、`GenerateConfigInputSchema`、`InitHarnessInputSchema` 均已有 `dryRun?: boolean`，handler 中也检查了该字段 | 结论：已实现，非缺口 |
| 状态机"无阶段顺序校验" | `confirm_decisions` handler（`index.ts:448`）有 `validStatuses` 数组校验 | 有基础校验，但无设计中要求的数值索引机制 |

其余 11 项缺口经代码确认属实。

---

## P0 — 核心流程缺口（影响正确性）

### P0-1: 冲突检测缺失

- **状态**: 新发现（`engine.ts` 无冲突逻辑，设计 §6.2 要求）
- **文件**: `mcp-server/src/engine.ts`
- **位置**: 在 `evaluate()` 方法末尾，`decisions` 生成之后（行 90）
- **当前代码**: `evaluate()` 直接返回 `{decisions, summary}`，无冲突检查
- **改动内容**:
  1. 在 `engine.ts` 中添加冲突矩阵（约 5 对已知冲突规则）
  2. 添加 `detectConflicts(decisions: RuleDecision[]): RuleConflict[]` 方法
  3. 在 `evaluate()` 返回值中添加 `conflicts` 字段
  4. 在 `EngineOutput` 类型（`types.ts:75`）中添加 `conflicts?: RuleConflict[]`
  5. 在 `RuleConflict` 类型（需新增）：`{ruleA, ruleB, type, resolution}`
- **验证方法**: 创建含冲突规则的测试输入，调用 `engine.evaluate()`，期望输出 `conflicts` 非空
- **历次报告**: `fix-1.0.md` 未涉及；`harness-design-comparison.md` 提及冲突检测是设计优势但未确认实现状态

---

### P0-2: 状态机使用 `status` 而非 `phase`

- **状态**: 新发现（`HarnessState.status` vs 设计 `HarnessState.phase`）
- **文件**: `mcp-server/src/types.ts`
- **位置**: 行 17 `HarnessStatus` 类型定义，行 134 `HarnessState.status`
- **当前代码**: `status: HarnessStatus`（联合类型 `null | "evaluated" | ...`）；`confirm_decisions`（`index.ts:448`）有数组包含校验，但无数值索引推进
- **改动内容**:
  1. `types.ts:134`: `status: HarnessStatus` → `phase: HarnessStatus`（仅重命名字段名，保持现有状态值）
  2. `state.ts:55-59`: `updateStatus()` 保持
  3. `state.ts:66,75,84,94`: 所有 `state.status =` → `state.phase =`
  4. `index.ts:449`: `state.status` → `state.phase`；`state_manager.load()` 返回兼容旧格式
- **验证方法**: 完整流程测试：`evaluate_rules → confirm_decisions → generate_config`，各阶段 `query_state` 返回正确的 `phase` 值
- **历次报告**: 首次出现

---

### P0-3: 错误码体系未实现

- **状态**: 新发现（设计 §5.1 要求结构化 `ErrorCode` 枚举）
- **文件**: `mcp-server/src/types.ts`（新增），`mcp-server/src/index.ts`（改造错误返回）
- **当前代码**: 所有错误返回自由文本，无标准格式
- **改动内容**:
  1. 在 `types.ts` 中添加：
     ```typescript
     export type ErrorCode =
       | 'STATE_NOT_FOUND'
       | 'STATE_PHASE_MISMATCH'
       | 'FILE_READ_ERROR'
       | 'FILE_WRITE_ERROR'
       | 'CONFIRM_REQUIRED';

     export interface HarnessError {
       code: ErrorCode;
       message: string;
       detail?: string;
       recoverable: boolean;
     }
     ```
  2. 在 `index.ts` 中替换所有 `{error: string}` 返回为 `{error: HarnessError}` 格式
  3. 受影响位置：`index.ts:227-231`（无决策错误）、`index.ts:455-460`（状态不允许）
- **验证方法**: 在错误状态下调用工具，检查返回中是否包含标准化的 `code` 字段
- **历次报告**: 首次出现

---

### P0-4: `evaluate_rules` 输出与设计命名不一致

- **状态**: 新发现（输出字段命名、缺失字段）
- **文件**: `mcp-server/src/types.ts`，`mcp-server/src/engine.ts`
- **当前代码**: `EngineOutput`（`types.ts:75`）使用 `decisions[]` 而非设计的 `recommendations[]`；缺少 `conflicts`、`evidence`、`errorMessage`、`adjustedCost`、`feedbackSpeed`
- **改动内容**:
  1. `types.ts:75-83`: 将 `EngineOutput` 改为同时保留 `decisions`（向后兼容）和 `recommendations` 别名（可另选）
     - 实际建议：不改命名（`decisions` 已在全项目使用），仅补充缺失字段
  2. `engine.ts:102-110`: `evaluate()` 返回值补充 `conflicts` 数组
  3. `types.ts:56-65`: `RuleDecision` 补充 `adjustedCost?`、`feedbackSpeed?`、`evidence?`
  4. `rules.json`: 补充 `evidence`、`errorMessage` 字段（设计 §7.1 要求）
- **验证方法**: 调用 `evaluate_rules`，输出包含 `conflicts` 字段；rules.json 读取后每条规则包含 `evidence`
- **历次报告**: 首次出现

---

### P0-5: `generate_config` 不写文件到磁盘

- **状态**: 新发现（设计 §3.1.3 规定 MCP 层负责文件写入）
- **文件**: `mcp-server/src/index.ts`
- **位置**: `case "generate_config":`（行 211-258）
- **当前代码**: 仅返回 `{files, summary}` 数据，不执行任何写操作
- **改动内容**:
  1. 在 `index.ts:247` 后添加写磁盘逻辑（非 dryRun 时）：
     ```typescript
     if (!input.dryRun) {
       // Backup existing files
       backupGeneratedFiles(input.projectDir);
       // Write files to disk
       for (const f of files) {
         const filePath = join(input.projectDir, f.path);
         mkdirSync(dirname(filePath), { recursive: true });
         writeFileSync(filePath, f.content, 'utf-8');
         // Set executable permission for hook files
         if (f.path.startsWith('.husky/')) {
           chmodSync(filePath, 0o755);
         }
       }
       stateManager.setConfigOutput(output);
     }
     ```
  2. `GenerateConfigOutput`（`types.ts:116-128`）：补充 `.files[].backupPath`、`errors[]`、`warnings[]`
- **验证方法**: 调用 `generate_config`（非 dryRun），检查磁盘上文件已创建；再次调用验证备份目录已生成
- **历次报告**: `fix-1.0.md` P1-4 提及生成器代码重复（已修复），但写入逻辑未在历次报告中讨论

---

## P1 — 用户体验改进

### P1-1: SKILL.md 补全交互编排

- **状态**: 新发现（设计 §9 要求 11 步工作流，当前仅 6 步）
- **文件**: `mcp-server/skill/SKILL.md`
- **当前内容**: 6 步简化版，仅有步骤标题和参数示例，缺少 AskUserQuestion 交互、断点续做、错误恢复引导
- **改动内容**:
  1. 恢复设计的 11 步工作流结构
  2. 加入 AskUserQuestion 话术模板
  3. 加入断点续做判断逻辑：`query_state` → 按 `phase` 跳过已完成步骤
  4. 加入错误恢复引导：验证失败 → rollback 建议
- **验证方法**: 评审 SKILL.md 内容是否覆盖所有设计步骤
- **历次报告**: 首次出现

---

### P1-2: `generate_config` 输出格式补齐

- **状态**: 新发现（设计 §3.1.3 要求 `action` 更多枚举值、`backupPath`、`errors`）
- **文件**: `mcp-server/src/types.ts`
- **位置**: `GenerateConfigOutput`（行 116-128）
- **当前代码**:
  - `action: "create" | "update" | "skip"`（缺少 `overwritten`、`merged`、`dry_run`）
  - 无 `backupPath` 字段
  - 无 `errors` / `warnings` 数组
- **改动内容**:
  1. `types.ts:120`: `action` 扩展为 `"create" | "overwritten" | "skipped" | "merged" | "dry_run"`
  2. `types.ts`: 添加 `backupPath?: string`
  3. `types.ts`: 添加 `errors: ConfigError[]`、`warnings: string[]`
- **验证方法**: 调用 `generate_config`，输出中 `files[n].action` 出现设计规定的枚举值
- **历次报告**: 首次出现

---

### P1-3: `validate_setup` 输出格式对齐

- **状态**: 新发现（设计 §3.1.4 使用 `status: pass/warn/fail`，当前使用 `summary.passed: boolean`）
- **文件**: `mcp-server/src/validators/setup_validator.ts`
- **位置**: `ValidationResult`（行 15-24）
- **当前代码**: `summary.passed: boolean`（true/false）
- **改动内容**: 补充 `summary.status: "pass" | "warn" | "fail"`，`passed` 保留作为兼容别名
- **验证方法**: 验证返回对象包含 `summary.status` 且值正确反映检查结果
- **历次报告**: 首次出现

---

### P1-4: `init_harness` 输入结构对齐设计

- **状态**: 新发现（设计使用 `preset: {techStack, projectPhase, teamSize}` 嵌套对象）
- **文件**: `mcp-server/src/types.ts`
- **位置**: `InitHarnessInputSchema`（行 232-238）
- **当前代码**: `projectPhase`、`teamSize`、`techStack` 为扁平参数
- **改动内容**: 补充 `preset` 嵌套字段作为可选别名，保持向后兼容：
  ```typescript
  preset: z.object({
    techStack: z.array(...),
    projectPhase: z.enum([...]),
    teamSize: z.enum([...]),
  }).optional(),
  ```
  注意：⚠️ 此改动可能破坏现有客户端调用。建议在 handler 中合并 `preset` 与扁平参数。
- **验证方法**: 两种调用方式均正常工作
- **历次报告**: 首次出现

---

## P2 — Phase 4 可选功能（按原设计外规划）

### P2-1: `assess_suitability` 工具

- **状态**: 反复出现（`harness-design-comparison.md` 已提及，设计 §3.1.9 有完整定义，类型 `SuitabilityAssessment` 在 `types.ts:157` 已存在但工具未注册）
- **文件**: `mcp-server/src/index.ts`（注册工具），新增 `src/suitability/assessor.ts`
- **类型状态**: `types.ts:148-163` 已有 `SuitabilityWarning`、`SuitabilityAssessment` 接口定义
- **改动内容**:
  1. 创建 `src/suitability/assessor.ts`，实现设计 §3.1.9 的评估逻辑（git 历史检查、文件结构分析）
  2. 在 `index.ts` 中注册 `assess_suitability` 工具
  3. 实现 handler：检查 git commits 数量、文件数量、依赖数量、测试文件存在性
- **验证方法**: 在一个小型项目中调用，返回 `suitable: false`；在一个完整项目中返回 `suitable: true`
- **历次报告**: `harness-design-comparison.md` 已识别为缺口

---

### P2-2: A/B 测试框架

- **状态**: 反复出现（`harness-design-comparison.md` 已提及，设计 §3.1.10-3.1.12 定义 3 个工具，类型 `ABTestConfig`/`ABTestResult` 在 `types.ts:168-193` 已存在但无 handler）
- **文件**: `mcp-server/src/index.ts`，新增 `src/ab_test/`
- **类型状态**: `types.ts:168-193` 已有 `ABTestConfig`、`ABTestDataPoint`、`ABTestResult`
- **改动内容**:
  1. 创建 `src/ab_test/manager.ts`（`start_ab_test` handler）
  2. 创建 `src/ab_test/collector.ts`（`collect_ab_metrics` handler）
  3. 创建 `src/ab_test/analyzer.ts`（`analyze_ab_results` handler）
  4. 在 `index.ts` 中注册 3 个工具
  5. 测试配置持久化到 `.harness/ab_tests.json`
- **验证方法**: 启动一个 A/B 测试 → 收集数据 → 分析结果，返回统计显著性
- **历次报告**: `harness-design-comparison.md` 已识别为缺口

---

### P2-3: 认知层 Skills 集成

- **状态**: 反复出现（`harness-design-comparison.md` 已提及，设计 §14 定义三类 Skills）
- **文件**: 新增 `mcp-server/src/cognitive_layer/`
- **改动内容**:
  1. 创建 `orchestrator.ts`（`CognitiveLayerOrchestrator`，设计 §14.3.3）
  2. 创建 `skills/diagnostic.ts`（诊断型 Skill）
  3. 创建 `skills/educational.ts`（教育型 Skill）
  4. 创建 `skills/decision_support.ts`（决策支持型 Skill）
  5. 在 `engine.ts:158-167` 已有 `cognitiveSkillTriggers` 生成逻辑，补充完整的上下文传递和 Skill 触发
- **验证方法**: 用户触发认知层请求后，返回对应的诊断/教育/决策支持响应
- **历次报告**: `harness-design-comparison.md` 已识别为缺口

---

### P2-4: 错误信息优化框架

- **状态**: 反复出现（`harness-design-comparison.md` 已提及，设计 §8.2 定义模板库系统）
- **文件**: 新增 `mcp-server/src/error_optimization/`
- **类型状态**: `types.ts:199-216` 已有 `ErrorMessageTemplate`、`ErrorSuggestion`
- **改动内容**:
  1. 创建 `templates.ts`（模板库，设计 §8.2.2）
  2. 创建 `generator.ts`（`ErrorMessageGenerator`，上下文字段填充）
  3. 创建 `evaluator.ts`（效果评估，模板切换建议）
- **验证方法**: 为 `no-explicit-any` 规则生成错误信息，输出包含 why/whatInstead/reference 三要素
- **历次报告**: `harness-design-comparison.md` 已识别为缺口

---

## 汇总

| 分级 | # | 描述 | 发现类型 | 涉及文件数 |
|------|---|------|---------|-----------|
| P0 | 1 | 冲突检测缺失 | **新发现** | 2 |
| P0 | 2 | 状态机 phase 命名 | **新发现** | 2 |
| P0 | 3 | 错误码体系未实现 | **新发现** | 2 |
| P0 | 4 | evaluate_rules 输出字段 | **新发现** | 3 |
| P0 | 5 | generate_config 不写文件 | **新发现** | 2 |
| P1 | 1 | SKILL.md 简化 | **新发现** | 1 |
| P1 | 2 | generate_config 输出格式 | **新发现** | 1 |
| P1 | 3 | validate_setup 输出格式 | **新发现** | 1 |
| P1 | 4 | init_harness 输入结构 | **新发现** | 1 |
| P2 | 1 | assess_suitability | **反复出现** | 2 |
| P2 | 2 | A/B 测试框架 | **反复出现** | 4 |
| P2 | 3 | 认知层 Skills | **反复出现** | 5 |
| P2 | 4 | 错误信息优化 | **反复出现** | 3 |

> **反复出现判断依据**: 在 `harness-design-comparison.md`（2026-04-22）和/或 `fix-1.0.md` 中已识别为缺口的项标记为"反复出现"，首次在本报告出现的标记为"新发现"。

---

## 验证清单

修复完成后，按以下顺序逐项验证：

### 编译 & 基础校验
- [ ] `npm run build` 通过，无 TS 编译错误
- [ ] `npm run lint` 输出 `0 errors`
- [ ] `npm test` 全部 300+ 测试通过

### P0 验证
- [ ] **冲突检测**: `evaluate_rules` 返回含冲突规则的决策时，输出包含 `conflicts` 数组
- [ ] **状态机**: `query_state` 返回的 phase 值依次为 `evaluated → confirmed → generated → validated`；尝试从 `null` 直接 `confirm_decisions` 返回错误
- [ ] **错误码**: 在无效状态调用工具，返回错误包含 `code` 字段（如 `STATE_PHASE_MISMATCH`）
- [ ] **输出命名**: `evaluate_rules` 输出包含完整的 `recommendations` 字段（或补充了 `evidence`、`adjustedCost` 等）
- [ ] **写文件**: `generate_config`（非 dryRun）在磁盘上创建 CLAUDE.md、eslint.config.json 等文件；再次调用验证备份目录存在

### P1 验证
- [ ] **SKILL.md**: 覆盖全部 11 步工作流流程
- [ ] **生成器输出**: `generate_config` 返回的 `.files[]` 包含 `backupPath` 和 `errors` 字段
- [ ] **校验器输出**: `validate_setup` 返回包含 `summary.status`
- [ ] **init_harness**: 支持 `preset: {...}` 嵌套参数调用

### P2 验证（按需）
- [ ] **assess_suitability**: 在小型项目中返回 `suitable: false`，在完整项目中返回 `suitable: true`
- [ ] **A/B 测试**: `start_ab_test → collect_ab_metrics → analyze_ab_results` 完整流程可执行
- [ ] **认知层**: 诊断型/教育型/决策支持型 Skills 均可触发
- [ ] **错误信息**: 生成的错误信息包含 why/whatInstead/reference 三要素

### 回归验证
- [ ] 完整流程走查：`evaluate_rules → confirm_decisions → generate_config → validate_setup`
- [ ] 备份回滚：修改文件 → `rollback` → 文件恢复原内容
- [ ] Pre-commit hook：`npx eslint . && npm test` 通过
