# 全面修复计划 — OpenAPI 规范 vs 实现差异 (v6)

**计划日期**: 2026-04-25 15:53
**依据**: `docs/verifications/gap-analysis-20260425-1553.md`
**验证方法**: 逐条对照 `mcp-server/src/` 当前源代码确认每项差异的真实状态

---

## 0. 前提：交叉验证结果

编制本计划前，逐条对照代码验证了 1553 报告的全部 7 项差异，并补充了报告中遗漏的 2 项。以下是对报告结论的修正：

| 报告叙述 | 代码实际状态 | 本计划处理 |
|---------|-----------|----------|
| §3.1 init_harness dryRun 输出不匹配 | 确认存在。dryRun 返回 `{phase, decisions, summary, message}`，非规约的 `{files, summary, validation}` | 纳入 P0-1 |
| §3.2 files 含 husky/CI 文件 | 确认存在。规约描述仅提 4 种文件，但响应包含额外 .husky/ 和 .github/ 文件。响应 schema 兼容 | 纳入 P2-2（仅文档更新） |
| §3.4 cognitive_skill 错误包装 | 确认存在。try/catch → `UNKNOWN_ERROR` 包装，规约预期结构化错误 | 纳入 P1-2 |
| §5.1 rateAfter 未使用 | 确认存在。schema 接受但 handler 不检查 | 纳入 P1-1 |
| §4.2 SuitabilityWarning.type 未使用的 cost | 确认存在。类型定义含 "cost"，但 assessor.ts 不产出 | 纳入 P2-1 |
| 报告中遗漏：错误响应包装 `{error: ...}` vs 规约的 flat HarnessError | **新发现**。所有 handler 使用 `{ error: { code, message, recoverable } }` 包装，规约响应 schema 引用 `HarnessError`（扁平结构无 `error` 键） | 纳入 P0-2 |
| 报告中遗漏：init_harness null/undefined 不一致 | **新发现**。`backupDir`/`hubSetup` 用 `undefined`（JSON 中消失），`recommendedActions` 用 `null`，规约三者均为 `nullable: true` | 纳入 P1-3 |
| 报告中遗漏：scan_codebase 默认值未在规约中记录 | 代码默认 `projectPhase: "early"`, `teamSize: "small"`, `techStack: ["generic"]`，规约未提及 | 纳入 P2-3 |

> **历次计划对照**: full-fix-1242 (v1)、1328 (v2)、1416 (v3)、1430 (v4)、1455 (v5) 中识别的 64 项差异已全部关闭。本计划 9 项均为 **新发现**（首次在当前系列报告中出现）。

---

## 历次报告对照

| 本计划编号 | 1242 (v1) | 1328 (v2) | 1416 (v3) | 1430 (v4) | 1455 (v5) | 状态 |
|-----------|-----------|-----------|-----------|-----------|-----------|------|
| P0-1 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P0-2 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P1-1 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P1-2 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P1-3 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P2-1 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P2-2 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P2-3 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |
| P2-4 | 未出现 | 未出现 | 未出现 | 未出现 | 未出现 | **新发现** |

**说明**: 由于 1455 (v5) 计划的 5 项已在当前代码中全部修复，本计划的 9 项均为全新识别的缺口。

---

## P0 — 结构性缺口（影响契约正确性）

### P0-1: `init_harness` dryRun 输出形状与规约不匹配

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 文件 | `mcp-server/src/index.ts` |
| 行号 | 491-503 |
| 当前代码 | dryRun 路径返回 `{phase: "evaluate", decisions: [...], summary: EngineOutput.summary, message: "..."}` |
| 规约预期 | 非 dryRun 和非 dryRun 输出一致结构 `{files: [...], summary: InitHarnessSummary, validation: ValidateSetupOutput}`（`openapi.yaml:243-251`） |
| 改动内容 | 修改 dryRun 分支使其输出与正常模式对齐：<br>1. 生成空 `files: []`（无实际文件生成）<br>2. 生成 `summary` 为 `{files: {total: 0, created: 0, updated: 0, skipped: 0}, decisions: N}`<br>3. 跳过 validation（无文件可验证），设 `validation: null` |
| 验证方法 | `curl/ts` 调用 `init_harness` 带 `"dryRun": true`，检查响应含 `files`/`summary`/`validation` 字段 |

### P0-2: 错误响应结构系统性地与规约不匹配

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 影响范围 | `mcp-server/src/index.ts` 中全部错误返回路径 |
| 行号 | 270-275, 629-634, 678-685, 716-721, 732-738, 836-840, 849-853, 880-889, 912-917, 958-963, 1021-1026 |
| 当前代码 | 全部使用 `{ error: { code, message, detail?, recoverable } }` 包装 |
| 规约预期 | 400 响应内容直接为 `$ref: "#/components/schemas/HarnessError"`（扁平结构 `{code, message, detail?, recoverable}`，无外层 `error` 键） |
| 改动内容 | 移除所有错误返回中的 `error:` 包装层，将 `HarnessError` 扁平化为响应顶层。示例：<br>`{ error: { code: "NO_DECISIONS", message: "...", recoverable: true } }` →<br>`{ code: "NO_DECISIONS", message: "...", recoverable: true }`<br>共涉及约 8 个 handler 的 13 处错误返回 |
| 验证方法 | 逐一触发各 handler 的错误条件，检查 JSON 响应顶层直接包含 `code`/`message`/`recoverable` 字段，而非 `{error: {...}}` |

---

## P1 — 功能合规缺口

### P1-1: `optimize_error_message` 忽略 `rateAfter` 参数

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 文件 | `mcp-server/src/index.ts` |
| 行号 | 1159-1183 |
| 当前代码 | handler 始终调用 `evaluator.recordSuggestion()` 记录评价，忽略 `optInput.rateAfter` |
| 改动内容 | 在第 1172 行的 `if (optInput.projectDir)` 条件中添加 `&& optInput.rateAfter !== false`：<br>```typescript<br>if (optInput.projectDir && optInput.rateAfter !== false) {<br>  const evaluator = new ErrorMessageEvaluator(optInput.projectDir);<br>  for (const s of suggestions) {<br>    evaluator.recordSuggestion(s, ...);<br>  }<br>  stats = evaluator.getStats();<br>}<br>``` |
| 验证方法 | 1. 调用 `optimize_error_message` 带 `"rateAfter": false`<br>2. 检查 `.harness/error_message_evaluations.json` 未新增记录<br>3. 去掉 `rateAfter` 或设 `true`，确认记录写入 |

### P1-2: `cognitive_skill` 异常时使用 try/catch + UNKNOWN_ERROR 而非结构化错误

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 文件 | `mcp-server/src/index.ts` |
| 行号 | 1117-1131 |
| 当前代码 | try/catch 捕获所有异常 → `{error: {code: "UNKNOWN_ERROR", message: String(e), recoverable: true}}` |
| 规约预期 | 规则不存在时返回 `{code: "INVALID_CONFIG", message: "...", recoverable: true}`，无效输入返回校验错误 |
| 改动内容 | 将笼统的 try/catch 拆分为：<br>1. 保持 Zod 校验异常由外层框架处理<br>2. `processCognitiveRequest` 调用改为单独 try/catch，仅捕获规则未找到异常：<br>   ```typescript<br>   let result: CognitiveResponse;<br>   try {<br>     result = processCognitiveRequest(cogInput);<br>   } catch (e) {<br>     const msg = String(e);<br>     const isRuleNotFound = msg.includes("not found");<br>     return {<br>       content: [{ type: "text", text: JSON.stringify({<br>         code: isRuleNotFound ? "INVALID_CONFIG" : "UNKNOWN_ERROR",<br>         message: msg,<br>         recoverable: true,<br>       }, null, 2) }],<br>       isError: true,<br>     };<br>   }<br>   ```<br>3. 同时应用 P0-2 的扁平化改动（`{code, message, recoverable}` 而非 `{error: {code, message, recoverable}}`） |
| 验证方法 | 1. 调用 `cognitive_skill` 带不存在的 ruleId，检查响应含 `code: "INVALID_CONFIG"`<br>2. 调用有效参数，确认正常返回 |

### P1-3: `init_harness` 输出中 `backupDir`/`hubSetup` 用 `undefined` 代替 `null`

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 文件 | `mcp-server/src/index.ts` |
| 行号 | 583-587 |
| 当前代码 | `backupDir: backupDir \|\| undefined`、`hubSetup: ... ? ... : undefined` `recommendedActions: depInfo`（使用 null） |
| 规约预期 | 三者均为 `nullable: true`（openapi.yaml:2211,2218,2214），预期 `null` 而非消失 |
| 改动内容 | 统一使用 `null`：<br>1. 第 583 行：`backupDir: backupDir \|\| undefined` → `backupDir: backupDir \|\| null`<br>2. 第 585-587 行：`hubSetup: hookEntries.length > 0 ? ... : undefined` → `hubSetup: hookEntries.length > 0 ? ... : null`<br>3. 第 584 行：`recommendedActions: depInfo` 已为 null，一致<br>同时需更新 `InitHarnessSummary` 型（types.ts 中无对应 interface，为动态对象，无需修改） |
| 验证方法 | 1. 在无 Git hooks 的项目中调用 `init_harness`，检查响应 JSON 含 `"hubSetup": null` 而非缺失字段<br>2. 在无备份需求时调用，检查 `"backupDir": null` |

---

## P2 — 清理与文档

### P2-1: `SuitabilityWarning.type` 含未使用的 `cost` 值

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 文件 | `mcp-server/src/types.ts` |
| 行号 | 254-255 |
| 当前代码 | `type: "prototype" \| "script" \| "cost" \| "overhead"` |
| 实际使用 | `assessor.ts` 中仅产出 `prototype`/`script`/`overhead` 三种类型，`cost` 从未产出 |
| 改动内容 | 从类型中移除 `"cost"`：`"prototype" \| "script" \| "overhead"`（3 值）。<br>同时更新 `openapi.yaml:1762` 的 `SuitabilityWarning.type` 枚举为 `[prototype, script, overhead]` |
| 验证方法 | 1. TypeScript 编译通过（`npm run build`）<br>2. 运行 `assess_suitability`，确认所有警告的 type 均在新的三值枚举内<br>3. 运行关联测试（`npx vitest run src/suitability/` 或相关测试） |

### P2-2: 更新 OpenAPI 中 `init_harness` 的工具描述

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 文件 | `docs/api/openapi.yaml` |
| 行号 | 216-219 |
| 当前描述 | "快捷入口：跳过交互，内部依次调用 evaluate_rules → 自动确认决策 → generate_config → validate_setup" |
| 实际行为 | 额外生成 husky hooks（`.husky/pre-commit`, `.husky/commit-msg`）和 CI workflow（`.github/workflows/ci.yml`），以及依赖检查 |
| 改动内容 | 更新 description 为：<br>"快捷入口：跳过交互，内部依次调用 evaluate_rules → 自动确认决策 → generate_config → validate_setup。<br>额外生成：.husky/ Git hooks、.github/workflows/ci.yml CI 流水线、package.json 依赖检查与合并。" |
| 验证方法 | 文档审查 |

### P2-3: 在 OpenAPI 中记录 `scan_codebase` 的默认值

| 维度 | 值 |
|------|-----|
| 历次记录 | **新发现** |
| 文件 | `docs/api/openapi.yaml` |
| 行号 | 187-191（`ScanCodebaseInput` schema 的 `techStack`/`projectPhase`/`teamSize` 字段） |
| 当前文档 | 仅标注"实现新增"，未说明未传值时的默认行为 |
| 实际行为 | 代码 `index.ts:430-434`：`projectPhase ?? "early"`, `teamSize ?? "small"`, `techStack ?? ["generic"]` |
| 改动内容 | 在三个字段的 `description` 末尾追加默认值说明：<br>- `techStack`: 追加 "不传时默认 ["generic"]"<br>- `projectPhase`: 追加 "不传时默认 "early""<br>- `teamSize`: 追加 "不传时默认 "small"" |
| 验证方法 | 文档审查 |

### P2-4: `scan_codebase` 的 `saveToState` 创建了引擎输出但未使用扫描结果

> 注意：这是目前代码中观察到的设计问题，非规约合规性缺口，仅供参考。

`scan_codebase` handler 在 `index.ts:439-458` 将扫描结果写入 state，但使用的是 `setEngineOutput`，会覆盖之前 `evaluate_rules` 的引擎输出。扫描结果的 `configOutput` 也会覆盖状态中的生成记录。建议在后续迭代中考虑状态管理策略。

**建议但不纳入当前修复范围。**

---

## 验证清单

修复完成后，按以下顺序逐项验证：

### P0 验证

- [ ] **P0-1 dryRun 输出形状**：
  - 调用 `init_harness` 带 `"dryRun": true`
  - 检查响应顶层有 `files`（数组）、`summary`（含 `files`/`decisions`）、`validation`（可为 `null`）
  - 调用相同参数不带 `dryRun`，检查输出结构一致（files 有实际内容）

- [ ] **P0-2 错误响应扁平化**（批量验证）：
  - `generate_config`：传空 `decisions: []`，检查错误顶层是 `{code, message, recoverable}` 非 `{error: {...}}`
  - `confirm_decisions`：在 `phase: null` 时调用，检查扁平化
  - `rollback`：在无备份时调用，检查扁平化
  - `get_rule_stats`：带 `collect: false` 且无数据，检查扁平化
  - `analyze_rule_adjustments`：无分析数据时，检查扁平化
  - `export_rules`：无引擎输出时，检查扁平化
  - `import_rules`：无效 presetId，检查扁平化
  - `cognitive_skill`：不存在 ruleId，检查扁平化
  - 所有错误响应检查 `isError: true` 保持

### P1 验证

- [ ] **P1-1 rateAfter 参数**：
  - 调用 `optimize_error_message` 带 `"rateAfter": false`，检查 `.harness/error_message_evaluations.json` 无新增记录
  - 调用不带 `rateAfter`，检查记录写入
  - 调用带 `"rateAfter": true`，检查记录写入

- [ ] **P1-2 cognitive_skill 结构化错误**：
  - 调用 `cognitive_skill` 带 `{"skillType": "diagnostic", "ruleId": "non-existent"}`，检查响应含 `"code": "INVALID_CONFIG"`
  - 调用 `cognitive_skill` 带有效参数，检查正常返回 CognitiveResponse

- [ ] **P1-3 init_harness null 统一**：
  - 在无 hooks、无备份条件的项目调用 `init_harness`，检查响应含 `"hubSetup": null` 和 `"backupDir": null`
  - 检查 `"recommendedActions"` 同样为 `null`（此前已一致）

### P2 验证

- [ ] **P2-1 SuitabilityWarning.type 精简**：
  - `npm run build` 编译无错误
  - `npx vitest run src/suitability/` 测试通过
  - `grep -r '"cost"' src/assessor.ts` 无结果
  - openapi.yaml 中 `SuitabilityWarning.type` 枚举为 `[prototype, script, overhead]`

- [ ] **P2-2 init_harness 文档更新**：
  - 审查 openapi.yaml:216-219 包含 `.husky/` 和 `.github/` 文件的描述

- [ ] **P2-3 scan_codebase 默认值文档**：
  - 审查 openapi.yaml 中 `ScanCodebaseInput` 的 `techStack`/`projectPhase`/`teamSize` 包含默认值说明

### 集成验证

- [ ] **编译检查**：`npm run build` 无 TypeScript 错误
- [ ] **测试通过**：`npm run test` 全部通过
- [ ] **Lint 检查**：`npm run lint` 无新增问题
