# OpenAPI vs 实现差异分析报告

**日期**: 2026-04-28
**分析范围**: `docs/api/openapi.yaml` vs `mcp-server/src/`
**测试基线**: 332/332 tests passing（P18 已实施）

---

## 差异项总览

| ID | 级别 | 位置 | 类型 | 说明 |
|----|------|------|------|------|
| G-01 | P0 | `InitHarnessSummary` schema | 字段缺失 | `permissions` 字段未在 OpenAPI 中定义 |
| G-02 | P0 | `init_harness` description | 描述过时 | "额外生成"列表未包含 `commitlint.config.js` |
| G-03 | P1 | `generate_config` description | 描述过时 | "四种核心配置文件"不准确；"module.exports = [...]" 硬编码已改为动态 ESM/CJS |
| G-04 | P1 | `ConfigFileResult.description` | 描述不完整 | 常见文件值列表未包含 `.lintstagedrc.json` 和 `commitlint.config.js` |
| G-05 | P2 | `RuleStats` schema | 字段缺失 | 实现中 `category?: string` 未在 OpenAPI 中定义 |
| G-06 | P2 | `export_rules` response | schema 过简 | `export` 字段仅标注 `type: object`，未定义 `RuleExportData` 结构 |
| G-07 | P2 | `SuitabilityWarning` description | 描述不一致 | description 说 evidence 是"实现新增，设计无此字段"，但不是设计差异而是 schema 缺漏 |

---

## G-01: InitHarnessSummary 缺少 permissions 字段

### OpenAPI 当前内容
```yaml
# L2347-2371
InitHarnessSummary:
  type: object
  properties:
    files:        { type: object, properties: { total, created, updated, skipped } }
    decisions:    { type: integer }
    backupDir:    { type: string, nullable: true }
    recommendedActions: { $ref: "#/components/schemas/DepCheckResult" }
    huskySetup:   { type: string, nullable: true, description: Husky 安装指引 }
```

### 实现当前内容
```typescript
// index.ts L595-616
const permissionHint = {
  note: "为提高效率，建议将常用 MCP 工具加入 permissions allowlist",
  command: "运行 /fewer-permission-prompts 或在 .claude/settings.local.json 中配置",
  suggestedTools: [
    "mcp__harness-automation__evaluate_rules",
    "mcp__harness-automation__generate_config",
    "mcp__harness-automation__query_state",
    "mcp__harness-automation__init_harness",
    "mcp__harness-automation__validate_setup",
  ],
};
const summary = {
  files, decisions, backupDir, recommendedActions, huskySetup,
  permissions: permissionHint,
};
```

### 根源
P18-6 已实施但 OpenAPI 未同步更新。

### 修复
在 `InitHarnessSummary.properties` 中新增可选字段：
```yaml
permissions:
  type: object
  description: 权限配置提示（P18-6 新增）
  properties:
    note:
      type: string
      description: 提示文本
    command:
      type: string
      description: 配置方法指引
    suggestedTools:
      type: array
      items:
        type: string
      description: 建议加入 allowlist 的 MCP 工具列表
```

---

## G-02: init_harness 描述未提及 commitlint.config.js

### OpenAPI 当前内容
```yaml
# L222-225
description: >
  额外生成：.husky/ Git hooks、.github/workflows/ci.yml CI 流水线、package.json 依赖检查与合并。
```

### 实现当前行为
```typescript
// index.ts L548-557
if (hasCommitMsgHook) {
  const commitlintPath = join(input.projectDir, "commitlint.config.js");
  if (!existsSync(commitlintPath)) {
    files.push({ path: "commitlint.config.js", content: generateCommitlintConfig(), action: "created" });
  }
}
```

当 `commit-msg hook` 存在时额外生成 `commitlint.config.js`。

### 修复
在 `init_harness` description 的"额外生成"列表中补充 `commitlint.config.js`。

---

## G-03: generate_config 描述过时

### OpenAPI 当前内容
```yaml
# L74-77
description: >
  生成 CLAUDE.md、ESLint 配置（eslint.config.js Flat Config 格式）、
  .claude/settings.json、.gitignore 四种核心配置文件。
  ESLint 配置输出为 Flat Config（module.exports = [...]）格式
```

### 实现当前行为
1. `generateProjectFiles` 实际生成 5 类文件（增加了 `.lintstagedrc.json`）
2. ESLint 输出为动态格式：ESM 项目输出 `export default [...]`，CJS 项目输出 `module.exports = [...]`

### 修复
```yaml
description: >
  生成 CLAUDE.md、ESLint 配置、.claude/settings.json、.gitignore、.lintstagedrc.json 等核心配置文件。
  ESLint 配置输出为 Flat Config 格式（根据项目 type 字段自动选择 CommonJS 或 ESM）。
```

---

## G-04: ConfigFileResult 常见文件值列表不完整

### OpenAPI 当前内容
```yaml
# L1586-1589
path:
  type: string
  description: >
    配置文件的相对路径。常见值：CLAUDE.md、eslint.config.js、
    .claude/settings.json、.gitignore、.husky/pre-commit、
    .husky/commit-msg、.github/workflows/ci.yml。
```

### 实现实际常见值
缺少：
- `.lintstagedrc.json`
- `commitlint.config.js`

### 修复
在 description 中补充缺失的两个文件路径。

---

## G-05: RuleStats 缺少 category 字段

### OpenAPI 当前内容
```yaml
# L1886-1900
RuleStats:
  type: object
  properties:
    ruleId:           { type: string }
    ruleName:         { type: string }
    medium:           { $ref: "#/components/schemas/RuleMedium" }
    confidence:       { type: number }
    cognitiveRequired: { type: boolean }
```

### 实现当前内容
```typescript
// rule_analytics.ts L9-16
export interface RuleStats {
  ruleId: string;
  ruleName: string;
  medium: Medium;
  confidence: number;
  cognitiveRequired: boolean;
  category?: string;  // 实现中存在，OpenAPI 未定义
}
```

### 根源
`category` 字段在实现中一直存在，由 `_inferCategory()` 生成，但 OpenAPI 从未同步。

### 修复
在 `RuleStats.properties` 中新增可选字段：
```yaml
category:
  type: string
  description: 规则类别（如 code-quality、architecture、security、process）
```

---

## G-06: export_rules 响应 schema 过简

### OpenAPI 当前内容
```yaml
# L478-484
properties:
  export:
    type: object
  savedPath:
    type: string
    nullable: true
```

### 实现实际结构
```typescript
// rule_io.ts L8-25
export interface RuleExportData {
  version: string;
  exportedAt: string;
  source: {
    projectDir: string;
    projectPhase?: string;
    teamSize?: string;
    techStack?: string[];
  };
  rules: Array<{
    ruleId: string;
    ruleName: string;
    recommendedMedium: Medium;
    confidence: number;
    alternativeMedia: Medium[];
    reasons: string[];
  }>;
}
```

### 修复
将 `export` 字段引用 `$ref` 到一个明确定义的 `RuleExportData` schema。

```yaml
RuleExportData:
  type: object
  properties:
    version:
      type: string
    exportedAt:
      type: string
      format: date-time
    source:
      type: object
      properties:
        projectDir: { type: string }
        projectPhase: { type: string }
        teamSize: { type: string }
        techStack:
          type: array
          items: { type: string }
    rules:
      type: array
      items:
        type: object
        properties:
          ruleId: { type: string }
          ruleName: { type: string }
          recommendedMedium: { $ref: "#/components/schemas/RuleMedium" }
          confidence: { type: number }
          alternativeMedia:
            type: array
            items: { $ref: "#/components/schemas/RuleMedium" }
          reasons:
            type: array
            items: { type: string }
```

---

## G-07: SuitabilityWarning description 不准确

### OpenAPI 当前内容
```yaml
# L1828-1830
SuitabilityWarning:
  properties:
    type:
      description: 实现使用 prototype/script/overhead，设计使用 prototype/one_off_script/...
    evidence:
      description: 实现新增，设计无此字段
```

### 实现当前内容
```typescript
// types.ts L255-260
export interface SuitabilityWarning {
  type: "prototype" | "script" | "overhead";
  severity: "low" | "medium" | "high";
  message: string;
  evidence?: string[];
}
```

### 问题
`evidence` 字段确实在 OpenAPI 中已定义（L1836-1838），所以 description 中 "实现新增，设计无此字段" 不准确——它已存在于 schema 中。该 description 应说明该字段是可选字段（已在实现中为 `optional`）。

### 修复
修正 `evidence` 字段的 description，移除"实现新增"的不准确说明。

---

## 已确认一致的部分

以下部分经核查实现与 OpenAPI 一致：

| Schema/工具 | 状态 | 备注 |
|-------------|------|------|
| 所有 22 个工具路径 | ✅ | 完全匹配 |
| `RuleMedium` 枚举 | ✅ | 8 值 + 3 别名 |
| `ProjectPhase` / `TeamSize` / `TechStack` | ✅ | 枚举值一致 |
| `HarnessStatus` | ✅ | null + 4 阶段 |
| `ErrorCode` | ✅ | 13 个错误码 |
| `HarnessError` | ✅ | code/message/detail/recoverable |
| `RuleDefinition` | ✅ | 含 cognitiveLayerSupport/errorMessage |
| `RuleDecision` | ✅ | 含 adjustedCost/adjustedCostLabel/feedbackSpeed/errorMessage |
| `RuleConflict` | ✅ | 含 description/resolution |
| `GenerateConfigOutput` | ✅ | files/summary/errors/warnings |
| `ConfigFileResult` | ✅ | path/content/action/backupPath（除常见值列表） |
| `EngineOutput` | ✅ | decisions/conflicts/summary |
| `QueryStateOutput` | ✅ | stateExists/phase/project/lastEvalAt 等 |
| `HarnessState` | ✅ | 完整匹配 |
| `ValidateSetupOutput` | ✅ | projectDir/findings/summary/nextSteps |
| `ValidationFinding` | ✅ | file/type/message/fix |
| `AnalyticsData` | ✅ | projectDir/collectedAt/summary/rules/history |
| `AdapterResult` | ✅ | projectDir/analyzedAt/summary/recommendations |
| `RuleRecommendation` | ✅ | 完整匹配 |
| `ABTestResult` / `ABTestConfig` / `ABTestDataPoint` | ✅ | 完整匹配 |
| `StartABTestResult` | ✅ | testId/status/startTime/endTime/message |
| `CognitiveResponse` (union) | ✅ | 3 种 skillType 结果 |
| `DiagnosticResult` / `EducationalResult` / `DecisionSupportResult` | ✅ | 完整匹配 |
| `CognitiveAutoTrigger` | ✅ | skillType/ruleId/topic/experienceLevel |
| `ErrorMessageTemplate` / `ErrorSuggestion` | ✅ | 完整匹配 |
| `EvaluationStats` | ✅ | totalRecords/ratedRecords/averageRating/helpfulRate/followRate/templateStats |
| `SuitabilityAssessment` | ✅ | suitable/score/reason/warnings/recommendations |
| `RulePreset` | ✅ | id/name/description/techStack/decisions |
| `RollbackOutput` | ✅ | status/restored/failed/errors/backupId/cleaned |
| `DepCheckResult` | ✅ | missing/outdated/installCommand/suggestedCommands |
| `ScanCodebaseOutput` / `ScanSuggestion` | ✅ | scanSummary/extractedRules/decisions |
| `InitHarnessInput` / `GenerateConfigInput` / `ConfirmDecisionsInput` | ✅ | 含 preset/dryRun |
| `CognitiveSkillInput` | ✅ | skillType/ruleId + 按 skillType 区分的参数 |
| AB test inputs | ✅ | start/collect/analyze 输入 schema |
| `optimize_error_message` response | ✅ | suggestions/stats/repeatedPattern |

---

## 修复优先级

| ID | 级别 | 建议 | 关联 |
|----|------|------|------|
| G-01 | P0 | 在 `InitHarnessSummary` 新增 `permissions` 对象 | P18-6 |
| G-02 | P1 | 更新 `init_harness` description 补充 `commitlint.config.js` | P18-4 |
| G-03 | P1 | 更新 `generate_config` description（ESM 说明 + 文件列表） | P18-3 |
| G-04 | P2 | 更新 `ConfigFileResult` description 常见值列表 | — |
| G-05 | P2 | 在 `RuleStats` 新增 `category` 字段 | — |
| G-06 | P2 | 定义 `RuleExportData` schema 替代匿名 `type: object` | — |
| G-07 | P2 | 修正 `SuitabilityWarning.evidence` description | — |

---

*Generated by Gap Analysis — 2026-04-28*
