interface:
  display_name: "Harness Automation"
  short_description: "Compile repository policy for consistent AI coding sessions"
  default_prompt: "Use $harness-automation to establish or verify this project's cross-session engineering constraints."
policy:
  allow_implicit_invocation: true
